<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Actionbar color -->
        <item name="colorPrimary">@color/toolBg</item>
        <!--Status bar color-->
        <item name="colorPrimaryDark">@color/toolBg</item>
    </style>

</resources>
